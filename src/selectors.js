export const selectCurrentKey = state => state.currentKey;
export const selectInput = state => state.input;
export const selectAccuracy = state => state.accuracy;
export const selectTimer = state => state.timer;
